# Development uses the same PostgreSQL RLS and schema validation as normal runtime.
# Run the owner migration first, then provide restricted SPRING_DATASOURCE_* credentials.
spring.jpa.hibernate.ddl-auto=validate
app.rls.enabled=true
